# DEPRECATED — build_dataset_lstm.py에 통합됨
# 이 파일은 더 이상 사용하지 않음
# seq_y 생성은 build_dataset_lstm.py에서 X, feat, labels와 함께 수행해 순서 정합성 보장
raise DeprecationWarning("build_seq_y.py는 build_dataset_lstm.py에 통합됨. build_dataset_lstm.py 실행 필요")
