# 프로젝트 정보

프로젝트에 관한 상세 정보는 @docs/프로젝트 구조.md 참고

# 문서 작성 규칙

- 문서(README, 주석 등)는 체언 종결 스타일로 작성 (예: "키워드를 수집", "UI 조작 없음")
- 체언 종결에는 마침표를 붙이지 않음
- 제목 등 헤딩도 체언 종결로 작성 (예시. "왜 ~인가x, ~인 이유o")
- 변수·수식이 등장하는 문서에서는 변수 정의와 데이터 흐름을 먼저 서술한 뒤 로직 설명
- 변수 설명은 표가 아닌 서술형으로 작성 (의미·목적·예시를 충분히 풀어서)

# 코드 주석 규칙

- `#` 주석만 사용, docstring(`"""`) 금지
- 꾸미기 배너(`# ── 제목 ──────`) 금지, `# 제목` 형태로 작성
- 주석에 개수·계산식 등 수치 넣지 않기
- 역할 설명 포함 — `# 무엇 — 어디서 어떻게 쓰이는지` 형태 (예: `# 교차상관 보조함수 — 최적 시차와 방향 동조율 산출`)

# 커밋 규칙

- Co-Authored-By 없이 사용자 단독 명의로 커밋
